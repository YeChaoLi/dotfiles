# 常用别名 (由 install.sh 软链到 ~/.aliases.zsh，在 .zshrc 末尾 source)
# 在 Debian/Ubuntu 上 fd 包名为 fdfind
command -v fdfind &>/dev/null && alias fd='fdfind'

# 示例:
# alias ll='ls -la'
# alias g=git
# alias v=nvim
